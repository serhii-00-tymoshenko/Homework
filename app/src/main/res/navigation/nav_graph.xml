<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/playlistFragment">

    <fragment
        android:id="@+id/playlistFragment"
        android:name="com.example.homework.ui.playlist.PlaylistFragment"
        android:label="Playlist"
        tools:layout="@layout/fragment_playlist">
        <action
            android:id="@+id/action_playlistFragment_to_singerFragment"
            app:destination="@id/singerFragment" />
    </fragment>
    <fragment
        android:id="@+id/singerFragment"
        android:name="com.example.homework.ui.singer.SingerFragment"
        android:label="Singer"
        tools:layout="@layout/fragment_singer" />
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.example.homework.ui.settings.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />
</navigation>